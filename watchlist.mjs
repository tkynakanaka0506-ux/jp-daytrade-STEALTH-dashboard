// ==================================================================
// watchlist.mjs — 手動ウォッチリスト（TDnet直近14営業日 ∪ SBI決算カレンダー
// のどちらでも発見できない銘柄を個別に追跡する）
//
// 実測ギャップ（ユーザー指摘、2026-09-10）: シマダヤ(250A)は1Q決算
// （原材料・エネルギー・物流費高で減益だが、通期予想は維持。10月の
// 価格改定でH2以降のマージン改善を見込むシナリオ）が既に開示済み
// だったが、開示から14営業日以上経過しTDnetの拾える範囲（smart_entry.mjs
// のbuildUniverse）から外れ、次回(2Q)決算もまだSBI決算カレンダーに
// 載っていなかった（sbi.mjsのloadEarningsCalendarは「発表がある日」を
// 列挙するだけで、発表日自体が未確定/未掲載の銘柄は一切拾えない）ため、
// SMART ENTRY・AMBUSHどちらの発見経路にも一度も乗らず、システムから
// 完全に見えなくなっていた。
//
// 「直近の開示は無いが、次の決算までの間に良いシナリオが確認できている」
// という、まさにこのダッシュボードが拾いたい状態の銘柄が、開示直後から
// 次回決算がAMBUSH窓（決算まで60日以内）に入るまでの数週間、機械的に
// 死角になる構造上の欠陥だった。
//
// buildUniverse（smart_entry.mjs）とloadEarningsCalendarのextraCodes
// （sbi.mjs、定義はあったが誰も渡していなかった未使用パラメータ）の
// 両方にこのリストを渡すことで、TDnet/SBIカレンダー側の「勝手に見つかる」
// のに頼らず個別に追跡できるようにする。US_TENBAGGER_WATCHLIST
// （us_tenbagger.mjs）と同じ「手動リサーチで追記していく」運用（自動
// 発見はしない、ユーザー承認済みの設計方針）。
// ==================================================================
export const MANUAL_WATCHLIST = [
  {
    code: '250A',
    name: 'シマダヤ',
    note: '1Q営業利益8.25億円（前年比▲21%、原材料・エネルギー・製造固定費・運賃高が要因）。通期営業利益予想37億円（+1.8%）は維持。10月の価格改定効果でH2〜3Qにかけての利益率改善を見込むシナリオ。',
  },
];

export const MANUAL_WATCHLIST_CODES = MANUAL_WATCHLIST.map((w) => w.code);

// ==================================================================
// テンバガー候補監視リスト（ユーザー提案 2026-09-13）
//
// 通常のテンバガー候補（Tier A/B、smart_entry.mjsのscanGrowthPrecursors）
// は東証グロース市場・時価総額300億〜1000億円のレンジでしか探して
// いない。TOWA(6315)は時価総額約1,591億円・東証プライムの半導体製造
// 装置メーカーで、このレンジに収まらないため自動発見の対象外（実測で
// 確認済み）。
//
// ユーザーの投資判断（2026-09-13）: 「信用買い残がかなり高いので、
// そこをクリアさえすれば買うつもり」。実データで確認済み: 直近の
// 信用買い残512.7万株は直近13週レンジの最高値（creditLevelVsRange=
// 100%）、信用倍率93.56倍（買い長）、4週前比+44.1%と急増中。この
// 「高すぎる信用買い残がクリアされるタイミング」を継続的に監視したい
// という要望のため、対象を決算日・成長率レンジ等の自動判定条件では
// 絞り込まず、ユーザーが個別に選定した銘柄の信用需給（週次信用残・
// 信用倍率・直近レンジ内の位置）だけを毎回追跡するリストにする。
// US_TENBAGGER_WATCHLIST/MANUAL_WATCHLISTと同じ「手動で追記していく」
// 運用（自動発見はしない）。
export const TENBAGGER_WATCHLIST = [
  {
    code: '6315',
    name: 'TOWA',
    note: '半導体製造装置（樹脂封止装置）大手。信用買い残が直近13週の高値圏に張り付いており、この需給がクリアされたら買いを検討する監視銘柄。',
  },
];

export const TENBAGGER_WATCHLIST_CODES = TENBAGGER_WATCHLIST.map((w) => w.code);
